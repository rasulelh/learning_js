# Format Array

Converts an array of numbers into a formatted string joined by arrows.

---

## 📌 Description

The function `formatArray` takes an array of numbers and converts it into
a string where all elements are joined using the `->` separator.

If the array is empty, the function returns an empty string.

The solution is implemented using a loop.

---

## 🧪 Example

```js
formatArray([10, 20, 30]); // "10 -> 20 -> 30"
formatArray([42]);        // "42"
formatArray([]);          // ""

```
## Topics
- JavaScript basics